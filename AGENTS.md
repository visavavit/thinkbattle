# Agent instructions

This project keeps one set of agent instructions, in [`CLAUDE.md`](./CLAUDE.md).

`AGENTS.md` and `CLAUDE.md` were previously byte-identical copies, which meant an
edit to one silently left the other as stale guidance. Read `CLAUDE.md`, and make
any changes there.
